__pycache__/
*.py[cod]
.venv/
venv/
.env
.pytest_cache/
.ruff_cache/

# local agent instructions — not part of the project
CLAUDE.md

# personal learning notes — not part of the project
learning_log.md

# live data pulls — the COMMITTED frozen copy lives in data/snapshot/
data/raw/

# regenerable from scripts/generate_synthetic.py (the 3 source CSVs + manifest are committed)
data/synthetic/base_transactions.json

# One ~480KB JSONL per run, timestamped — regenerate with scripts/run_pipeline.py.
# outputs/reports/ IS committed: it is the evidence for the metrics we quote.
outputs/audit/*.jsonl
!outputs/**/.gitkeep

# ChromaDB vector store — rebuilt from data/policy/ on first run (or via
# scripts/build_rag_index.py). Binary + non-deterministic, so not committed.
data/rag_index/
