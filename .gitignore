__pycache__/
*.py[cod]
.venv/
venv/
.env
.pytest_cache/
.ruff_cache/

# local agent instructions — not part of the project
CLAUDE.md

# personal learning notes — not part of the project
learning_log.md

# live data pulls — the COMMITTED frozen copy lives in data/snapshot/
data/raw/

# regenerable from scripts/generate_synthetic.py (the 3 source CSVs + manifest are committed)
data/synthetic/base_transactions.json

# generated at runtime
outputs/audit/*.jsonl
outputs/reports/*
!outputs/**/.gitkeep

# local vector store build (rebuildable from data/policy/)
data/policy/.chroma/
