"""LLM client wrapper for the reasoning layer.

TODO (Day 3): structured-output call ({decision, confidence, rationale}) for a
single ambiguous record + its match candidates. Must support a cached/replay
mode so the pipeline runs from a clean clone with no API key (see docs).
"""
