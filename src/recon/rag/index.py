"""Build and load the GST/tax policy vector store.

TODO (Day 4): chunk 5-10 real policy docs from data/policy/, embed, persist
the index under data/ so a clean clone can load it without rebuilding.
"""
