"""Tolerant fuzzy matching (amount rounding, date windows, name similarity).

TODO (Day 2): produce candidate matches with a score; classify each record as
matched | unmatched-ambiguous | unmatched-exception per config thresholds.
"""
