"""Component 5 — Agent Orchestration.

Drives ingest -> match -> reason -> ground -> log. Retries transient failures,
escalates anything unresolved, and demonstrates at least one deliberately
injected failure mode handled gracefully.
"""
